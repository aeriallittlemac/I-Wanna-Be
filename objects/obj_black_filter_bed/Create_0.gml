depth = FILTER_DEPTH
filter_speed = 0.005
image_alpha = 0;
filter_mode = "transparent"
filter_timer = 4.5;
timer = 0;

object_set_visible(self,true);

// Comment out the above line and use this instead if on Ubuntu Beta.
// visible = true;

//bgm_gain = audio_sound_get_gain(bobbys_room);